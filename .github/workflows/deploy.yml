on:
  push:
    branches:
      - main
jobs:
  docusaurus-deploy:
    runs-on: windows-latest
    steps:
      - run: |
          dir
          dir /b
          dir /b /s
          cmd /C 'set "GIT_USER=ZHENG28" && yarn deploy'
